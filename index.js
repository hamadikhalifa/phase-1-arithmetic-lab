// Declare num1 and num2 with values that multiply to 62
const num1 = 2;
const num2 = 31;

// Multiply num1 and num2
const multiply = num1 * num2;

console.log(multiply); // Output should be 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1;

console.log(random); // Output should be a random number greater than 0

// Declare num3 and num4 so that num3 % num4 equals 4
const num3 = 16;
const num4 = 6;

// Calculate the remainder
const mod = num3 % num4;

console.log(mod); // Output should be 4

// Find the maximum number in a set
const max = Math.max(1, 5, 10, 15, 20);

console.log(max); // Output should be 20